import { useState } from 'react';
import { CircleMarker, MapContainer, Popup, TileLayer } from 'react-leaflet';
import { Layers } from 'lucide-react';
import 'leaflet/dist/leaflet.css';
import type { FleetBus,UrbanEvent } from '../types';
const colors={critical:'#ff526a',high:'#ff9d42',medium:'#f6cf56',low:'#3bd7c5'};
export function CityMap({events,buses,onSelect,compact=false}:{events:UrbanEvent[];buses:FleetBus[];onSelect:(e:UrbanEvent)=>void}){const [layers,setLayers]=useState({events:true,buses:true});return <div className={'city-map '+(compact?'compact':'')}><MapContainer center={[28.6139,77.209]} zoom={12} scrollWheelZoom style={{height:'100%',width:'100%',filter:'saturate(.55) brightness(.7) contrast(1.15)'}}><TileLayer attribution="© OpenStreetMap contributors" url="https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png"/>{layers.buses&&buses.map(b=><CircleMarker key={b.id} center={[b.lat,b.lng]} radius={7} pathOptions={{color:'#e8ffff',weight:2,fillColor:'#36c7cf',fillOpacity:1}}><Popup><b>{b.id}</b><br/>{b.route}<br/>{b.speed} km/h</Popup></CircleMarker>)}{layers.events&&events.map(e=><CircleMarker key={e.id} center={[e.lat,e.lng]} radius={e.severity==='critical'?11:9} pathOptions={{color:'#fff',weight:2,fillColor:colors[e.severity],fillOpacity:1}} eventHandlers={{click:()=>onSelect(e)}}><Popup><b>{e.type}</b><br/>{e.location}<br/>Confidence: {e.confidence}%</Popup></CircleMarker>)}</MapContainer><div className="layer-switch"><button className="layers"><Layers size={15}/> Layers</button><div className="layer-menu"><label><input type="checkbox" checked={layers.buses} onChange={e=>setLayers(x=>({...x,buses:e.target.checked}))}/> Buses</label><label><input type="checkbox" checked={layers.events} onChange={e=>setLayers(x=>({...x,events:e.target.checked}))}/> AI events</label></div></div><div className="map-legend"><b>LIVE LAYERS</b><span><i className="dot bus-dot"/> Buses</span><span><i className="dot"/> AI events</span></div></div>}
